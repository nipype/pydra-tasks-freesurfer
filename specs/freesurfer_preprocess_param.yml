ReconAll:
  output_requirements: [T1_files, subjects_dir, subject_id]
  output_templates:
    # transformed_file: "{source_file}_warped"
  doctest:
    T1_files: "structural.nii"
    subjects_dir: "."
    subject_id: "foo"
    cmdline: "recon-all -all -i structural.nii -qcache -subjid foo -sd ."
  tests_inputs: []
  tests_outputs: []
